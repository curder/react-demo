import { memo } from 'react'
import type { FC, ReactNode } from 'react'

interface DjradioProps {
  children?: ReactNode
}

const Djradio: FC<DjradioProps> = (props) => {
  return <>Djradio</>
}

export default memo(Djradio)

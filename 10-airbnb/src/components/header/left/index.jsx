import React, { memo } from 'react'
import { LeftContainer } from './style'

const Left = memo(() => {
    return (
        <LeftContainer>
            <div>Left</div>
        </LeftContainer>
    )
})

export default Left
import React, { memo } from 'react'
import BannerContainer from './style'

const Banner = memo(() => {
    return (
        <BannerContainer>
            <div>Banner</div>
        </BannerContainer>
    )
})

export default Banner 